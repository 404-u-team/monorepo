FROM oven/bun:1.1.20


RUN apt-get update && apt-get install -y \
    nodejs \
    npm \
    && rm -rf /var/lib/apt/lists/*

WORKDIR /app

COPY package.json bun.lock* ./

RUN bun install

COPY . .

EXPOSE 6006

CMD ["bun", "run", "storybook", "--port", "6006"]