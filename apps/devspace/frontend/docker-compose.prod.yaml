services:
    frontend-prod:
        build:
            context: .
            dockerfile: Dockerfile.prod
        env_file:
            - .env.stand
        ports:
            - "5173:5173"
        restart: unless-stopped
