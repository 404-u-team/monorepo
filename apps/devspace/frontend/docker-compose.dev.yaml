services:
    frontend-dev:
        build:
            context: .
            dockerfile: Dockerfile.dev
        env_file:
            - .env.stand
        ports:
            - "5173:5173"
        command: bun run dev --host 0.0.0.0
